<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base theme. Compose paints the real UI; this only needs to be a
         no-action-bar, edge-to-edge friendly shell. Dark mode is handled by
         Compose plus the values-night window_background override. -->
    <style name="Theme.YoutubeArchiver" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Launch theme: a branded splash (core-splashscreen) that hands off to the
         main theme once the app is ready. -->
    <style name="Theme.YoutubeArchiver.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.YoutubeArchiver</item>
    </style>
</resources>
